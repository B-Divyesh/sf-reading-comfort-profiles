[
  {
    "id": "sample-demo",
    "claim": "Visitors can try a seeded sample workspace in one click without changing real profile data.",
    "where": "landing first screen, /demo, README",
    "test": "npm run test:e2e -- --grep @claim:sample-demo --project desktop-chromium",
    "sandbox": "fresh browser context; click the landing action; assert seeded work data and only the demo-prefixed storage key"
  },
  {
    "id": "profile-settings",
    "claim": "The sample changes text, code, contrast, and table settings and can be reset.",
    "where": "/demo, landing product description, README",
    "test": "npm run test:e2e -- --grep @claim:profile-settings --project desktop-chromium",
    "sandbox": "fresh /demo context; change and persist a setting; reload; reset; leave demo and assert demo data is discarded"
  },
  {
    "id": "privacy-local",
    "claim": "The website demo has no analytics, cookies, accounts, or third-party runtime requests and uses only demo-prefixed local storage.",
    "where": "landing facts, /demo, /privacy, README",
    "test": "npm run test:e2e -- --grep @claim:privacy-local --project desktop-chromium",
    "sandbox": "intercept every request during the complete demo flow; assert same-origin only, no cookies, and only the demo storage namespace"
  },
  {
    "id": "offline-reload",
    "claim": "The product site and interactive demo reload offline after the first visit.",
    "where": "landing facts, offline notice, /privacy, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload --project desktop-chromium",
    "sandbox": "fresh context; activate the service worker; reload under browser offline mode; assert CSS and demo behavior"
  },
  {
    "id": "free-download",
    "claim": "The Chromium extension is free to download and requires no account or payment.",
    "where": "landing, README",
    "test": "npm run test:e2e -- --grep @claim:free-download --project desktop-chromium",
    "sandbox": "fresh landing page; assert the no-account fact and unzip the same-origin downloadable package"
  },
  {
    "id": "keyboard-shortcuts",
    "claim": "The packaged extension provides commands to pause or resume and cycle profiles.",
    "where": "landing keyboard section, popup, README",
    "test": "npm run test:e2e -- --grep @claim:keyboard-shortcuts --project desktop-chromium",
    "sandbox": "download and unpack the release archive; assert all three command IDs and advertised default bindings in its manifest"
  },
  {
    "id": "per-domain-profiles",
    "claim": "The extension applies a selected reading profile to the current domain and can pause it there.",
    "where": "landing product sections, popup, README",
    "test": "npm run test:e2e -- --grep @claim:per-domain-profiles --project desktop-chromium",
    "sandbox": "fresh persistent Chromium profile with the packaged extension; apply Code focus to the sample host and pause it"
  },
  {
    "id": "extension-privacy",
    "claim": "The extension stores one local settings record, requests no history permission, and sends no third-party requests during use.",
    "where": "landing facts, /privacy, README",
    "test": "npm run test:e2e -- --grep @claim:extension-privacy --project desktop-chromium",
    "sandbox": "fresh persistent Chromium profile; run the extension flow; inspect local extension storage, manifest permissions, and all HTTP requests"
  }
]
